// TurnCoordinator tests require mocking the ST environment (getStContext, getSettings, etc.)
// This is deferred until the test infrastructure supports ST mocking.
// Manual verification: run SillyTavern, enable extension, observe console logs.
test('placeholder — coordinator tests need ST environment mocking', () => {
  expect(true).toBe(true);
});
